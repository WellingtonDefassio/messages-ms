spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5433/messages
    username: root
    password: secret
  jpa:
    hibernate:
      ddl-auto: update

clients:
  client-ms: ${CLIENT_MS:localhost:8080}